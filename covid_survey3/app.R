library(shiny)


ui <- fluidPage(

    # Application title
    titlePanel("Covid Survey 3")

)

server <- function(input, output) {

    
    
}

# Run the application 
shinyApp(ui = ui, server = server)
